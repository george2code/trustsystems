<h1>Invite your customers</h1>

<div class="btn-group" role="group" aria-label="...">
  <%= link_to 'Add Invites', send_invitations_add_invites_path, :class => 'btn btn-info' %>
  <button type="button" class="btn btn-default">Send from</button>
  <button type="button" class="btn btn-primary">Select template</button>
  <button type="button" class="btn btn-default">Done</button>
</div>

<h2>Create your invitation email</h2>
<p>Use our standard invitation email template or customize it to fit your brand. You can modify the text, color, font,
  and add your own logo. Read instructions.</p>
<br />

<%= form_for @afs, url: wizard_path, method: :put do  |f| %>
    <div class="form-group">
      <%= f.label :email_subject %>
      <%= f.text_field :email_subject, class: 'form-control', :disabled => true %>
    </div>

    <div class="form-group">
      <!-- model.Content -->
      <%= f.label :email_template_text %>
      <%= @afs.email_template_text.html_safe %>
    </div>

    <div class="row">
      <div class="col-md-3">
        <%= link_to 'Back', previous_wizard_path, :class => 'btn btn-warning' %>
      </div>
      <div class="col-md-9 text-right">
        <a href="javascript:void(0);" class="btn btn-info">Send test email to sale@ts.com</a>
        <%= f.submit 'Send invitations', class: 'btn btn-success' %>
      </div>
    </div>
<% end %>

<br />
<br />